# Domain Agent: Dashboard Specialist
> **Role**: Trang chủ hiển thị KPIs tổng quan và hoạt động gần đây.
> **Module**: `dashboard` (Home)
> **UI/UX**: Follow **Linear Design System** (Dark Mode, Keyboard-first, Skeleton Loaders)

## 1. Core Responsibilities
- Hiển thị KPI Cards real-time.
- Activity Feed (Hoạt động gần đây).
- Quick Actions (Tạo Báo giá, Xem Đơn hàng).
- Responsive & Accessible.

## 2. KPI Definitions
| KPI | Formula | Icon | Color |
| :--- | :--- | :--- | :--- |
| **Doanh Thu Tháng** | SUM(orders.total) WHERE month=current | 💰 | `#4ADE80` (success) |
| **Đơn Hàng Mới** | COUNT(orders) WHERE status='PENDING' | 📦 | `#5E6AD2` (accent) |
| **Khách Hàng** | COUNT(customers) | 👥 | `#8B5CF6` (purple) |
| **Tiệc Sắp Tới** | COUNT(events) WHERE date > NOW() | 📅 | `#FACC15` (warning) |

## 3. UI Components (Linear-style)
```
┌─────────────────────────────────────────────────────────────┐
│  [Logo]  Dashboard                        [Cmd+K] [User]    │ ← Top Bar
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│   │ 💰 Rev   │ │ 📦 Ord   │ │ 👥 Cust  │ │ 📅 Events│       │
│   │ 150M đ   │ │ 5        │ │ 32       │ │ 8        │       │
│   │ +12%     │ │ +2       │ │ +5       │ │ Hôm nay  │       │
│   └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│                                                             │
│   ┌─────────────────────────────────────────────────────┐   │
│   │ Activity Feed                                       │   │
│   │ • Đơn hàng #123 vừa được xác nhận      - 2 phút    │   │
│   │ • Báo giá mới từ Chị Lan               - 10 phút   │   │
│   │ • Nhân viên A check-in tiệc Quận 7     - 30 phút   │   │
│   └─────────────────────────────────────────────────────┘   │
│                                                             │
│   ┌─────────────────────────┐ ┌─────────────────────────┐   │
│   │ Quick Actions           │ │ Upcoming Events         │   │
│   │ [+ Báo Giá] [+ Đơn]     │ │ • Tiệc ABC - 15/01     │   │
│   └─────────────────────────┘ └─────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 4. Business Rules
- KPIs tự động refresh mỗi 30 giây (hoặc WebSocket).
- Activity Feed giới hạn 10 items gần nhất.
- Skeleton Loader khi đang load (KHÔNG dùng spinner).
- Keyboard: `R` để refresh, `C` để tạo báo giá.

## 5. API Endpoints
- `GET /dashboard/overview`: KPI data.
- `GET /dashboard/activity`: Activity feed.
- `GET /dashboard/upcoming-events`: Events list (mini).
